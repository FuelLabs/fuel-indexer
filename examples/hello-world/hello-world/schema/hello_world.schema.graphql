type Transaction @entity {
    id: ID!
    hash: Bytes32! @unique
}

type Block @entity @queryable {
    id: ID!
    height: U64! @search @unique
    hash: Bytes32! @unique
    transactions: [Transaction!]!
}
