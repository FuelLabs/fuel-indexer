schema {
    query: QueryRoot
}

type QueryRoot {
    message: Message
}

type Account {
    id: Address! @unique
    last_seen: Timestamp!
}

type Message {
    id: Bytes32! @unique
    sender: Account!
    receiver: Account!
    text: Charfield!
    last_seen: Timestamp!
}
