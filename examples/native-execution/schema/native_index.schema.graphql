schema {
    query: QueryRoot
}

type QueryRoot {
    account: Account
    message: Message
}

type Account {
    id: ID!
    address: Address! @unique
    last_seen: Timestamp!
}

type Message {
    id: ID!
    sender: Account!
    receiver: Account!
    text: Charfield!
    last_seen: Timestamp!
}
