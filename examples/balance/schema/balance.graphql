schema {
    query: QueryRoot
}

type QueryRoot {
    balance: Balance
}

type Balance {
    id: ID!
    amount: UInt8!
    timestamp: UInt8!
}

type AddBalance {
    id: ID!
    address: Address! @indexed
    timestamp: UInt8!
    amount: UInt8!
}