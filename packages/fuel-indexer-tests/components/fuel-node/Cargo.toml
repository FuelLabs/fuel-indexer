[package]
name = "fuel-node"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
clap = { features = ["cargo", "derive", "env"], workspace = true }
fuel-indexer-tests = { version = "0.0.0", path = "./../../../fuel-indexer-tests" }
fuel-types = { workspace = true }
fuels = { features = ["fuel-core-lib"], workspace = true }
tokio = { features = ["macros", "rt-multi-thread"], workspace = true }
